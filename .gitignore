__pycache__/
*.py[cod]
.pytest_cache/
logs/

# colcon build artifacts (generated at the repo root; never tracked)
/build/
/install/
/log/

# skill-manager는 별도 도구 clone (nested .git/build/install 추적 금지; wrapper만 커밋)
/tools/ros2-skill-manager/
